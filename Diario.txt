20190916  aag

Creado un usuario en github (Antgonz16)
Creado repositorio: ted con README.md y .gitignore (plantilla de java)

Clonar el local    git clone (url) ---> En cada equipo que querramos tener.


COMANDOS UTILIZADOS:
   1  apt install git
    2  sudo apt install git
    3  firefox
   11  pwd
   12  cd ~/
   13  cd ~\
   16  git clone https://github.com/Antgonz16/ted.git--> Obtenemos otro remoto en el local.
   17  cd ed
   18  cd ted
   23  git status
   24  git add d
   25  git add Diario.txt 
   26  git status
   27  git config --global user.name Antgonz16
   28  git config --global user.email antgon@alu.iesserpis.org
   29  git commit -m "Diario.txt con datos de github y git"
   30  git log
   31  git status
   32  git push
   33  git config --global push.default simple

git branch --> Vemos las diferentes ramas en los repositorios.
git branch --v > Sirve para ver la versión del repositorio local.
git checkout --> Cambiar la versión del repositorio.
git checkout master --> Para actualizar la versión del repositorio.


20190919

   27  git config --global user.name Antgonz16
   28  git config --global user.email antgon@alu.iesserpis.org
   29  git config --global push.default simple

       GIT ADD DIARIO.TXT 

   30  GIT COMMIT -M "DIARIO.TXT CON DATOS DE GITHUB Y GIT" --> ES UNA DESCRIPCIÓN DE LO QUE SUBIMOS AL REPOSITORIO.

   31  git log--> Muestra la lista de versiones.
   32  git status--> Estado determinado de las versiones.

   33  GIT PUSH--> SE ENCARGA DE PUBLICAR EN EL REMOTO. 

   34  git diff--> Muestra todos los cambios añadidos.

20190923
VirtualBox
Serpis dam: 2048MB RAM, DISCO 20GB, 2 PROCESADORES

sudo -i: Pasamos a root
apt update--> Descargamos una serie de paquetes.
apt full-upgrade--> Instalamos todos los paquetes que están disponibles.
gcc -o holamundo (nombre de salida) holamundo.c (fichero a compilar)
cat holamundo.c (Lee lo que hay dentro del programa)
./holamundo (nombre de salida)--> Ejecuta el programa
gcc -E HolaMundo.c --> Resuelve las directrices de preprocesamiento.
gcc -S HolaMundo.c -->

20190926

20190930

Hemos creado un holamundo de Java y a la vez lo hemos compilado.
Primero hemos escrito un HolaMundo siguiendo una sintaxís y después con un comando lo hemos compilado.

20191010
sudo apt install openjdk-8-jdk (Para instalar la version que queramos de Java).

